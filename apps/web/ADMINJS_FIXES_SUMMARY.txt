Admin.js Bugfixes Summary
=========================
Date: 2026-05-07
File: frontend/src/pages/Admin.js (4219 → 4288 lines)

All 6 bugs fixed. Build passes cleanly (react-scripts build).

────────────────────────────────────────────────────────────
Fix 1: renderMarkdown — Invalid HTML (li ohne wrapping)
────────────────────────────────────────────────────────────
BEFORE: Regex chain on escaped HTML produced broken output:
  - <li> without wrapping <ul>/<ol>
  - </p><p> injected inside <li> and heading tags
  - No block-level awareness

AFTER: Block-based parser:
  - Splits text by double-newlines into blocks
  - Each block classified: heading, code fence, unordered list,
    ordered list, or paragraph
  - Lists are wrapped in <ul>/<ol> with proper open/close
  - Inline formatting (bold, code) applied per-line with escapeHtml
  - No cross-contamination between block and inline processing

────────────────────────────────────────────────────────────
Fix 2: Hardcoded model name (line 3734)
────────────────────────────────────────────────────────────
BEFORE: <span>OpenRouter deepseek-v4-flash</span>
AFTER:  <span>{nxStatus?.openrouter?.model || 'OpenRouter'}</span>

Now reads dynamically from nxStatus state (populated by 
/api/admin/nexify-ai/status), with fallback to 'OpenRouter'.

────────────────────────────────────────────────────────────
Fix 3: Hardcoded placeholder model (line 2079)
────────────────────────────────────────────────────────────
BEFORE: placeholder="trinity-large-preview"  (model doesn't exist)
AFTER:  placeholder="deepseek/deepseek-v4-pro"

────────────────────────────────────────────────────────────
Fix 4: Debouncing for search inputs
────────────────────────────────────────────────────────────
BEFORE: leadsSearch and custSearch fired API on every keystroke
  via useEffect deps [..., leadsSearch, ...] and [..., custSearch, ...]

AFTER: Added debouncedLeadsSearch and debouncedCustSearch states.
  Two new useEffect hooks (300ms setTimeout/clearTimeout) debounce
  the raw input values into the debounced states.
  Fetch useEffects now depend on debounced values instead of raw.
  UI remains responsive (input shows typed text immediately).

────────────────────────────────────────────────────────────
Fix 5: loadApiKeys stale closure (line 3091)
────────────────────────────────────────────────────────────
BEFORE: useCallback(async () => { ... }, [])
  - Empty dep array captures stale apiFetch reference
  - apiFetch depends on [headers] → [token], so it can change

AFTER:  useCallback(async () => { ... }, [apiFetch])
  - Properly updates when apiFetch changes (e.g., token rotation)

────────────────────────────────────────────────────────────
Fix 6: Hardcoded placeholder domain (line 1855)
────────────────────────────────────────────────────────────
BEFORE: {c.contact?.email?.replace('@placeholder.nexifyai.de','') || ...}
  - Strips a domain that may not exist anymore

AFTER:  {c.contact?.email || c.contact?.first_name || '—'}
  - Shows full email as-is, no hardcoded domain stripping

────────────────────────────────────────────────────────────
Build Result: SUCCESS
  main.js: 515.51 kB (+270 B from original)
  No new warnings or errors.
