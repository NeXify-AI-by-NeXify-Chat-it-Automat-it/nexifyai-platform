services:
  frontend:
    image: nginx:alpine
    container_name: nx-frontend
    ports:
      - "80"
    volumes:
      - ./build:/usr/share/nginx/html:ro
    restart: unless-stopped
    networks:
      - proxy
networks:
  proxy:
    external: true
    name: proxy
